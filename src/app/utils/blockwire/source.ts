/** Where our source lives.
 *
 *  AGPL-3.0 §13 entitles a network user to the corresponding source of the
 *  version they are running — which is this fork, not the upstream project it
 *  came from. Linking upstream instead would be both worse branding and worse
 *  compliance, so every "source" link in the app resolves here.
 *
 *  This repository must stay PUBLIC and actually contain the source that
 *  built the running app. If it ever goes private or falls behind, the
 *  written offer on the /source page is what keeps us honest — and it has to
 *  be answered when someone asks.
 */
export const BLOCKWIRE_SOURCE_URL = 'https://github.com/stormchaserog/blockwire';
