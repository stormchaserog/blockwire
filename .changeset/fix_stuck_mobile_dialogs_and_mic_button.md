---
default: patch
---

Option-picker dialogs on mobile can now be dismissed by tapping outside them (previously iOS and mobile-web users were stuck), and a quick tap on the mic button no longer sends a recording the hold-to-record gesture had already discarded
