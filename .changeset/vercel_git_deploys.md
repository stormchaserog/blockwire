---
default: patch
---

Fixed the Vercel build so deploying from the repository produces the built app
rather than the source tree, and made the Matrix delegation files part of the
build instead of a manual copy step
