---
default: patch
---

The home conversation list now actually re-sorts as messages arrive instead of freezing in the order it had at startup, and bursts of member joins or power-level changes no longer re-render the whole room view for every single event
