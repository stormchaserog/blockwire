---
default: patch
---

Fixed broken styling that ignored the theme: the nickname editor and account-switch checkmark referenced CSS variables that don't exist, settings notices used hardcoded Material blue and orange, and the notifications empty state now matches the rest of the inbox
