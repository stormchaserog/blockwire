---
default: patch
---

More mobile polish: the room header title truncates instead of pushing the
action icons off-screen, the bottom tab bar clears the home indicator on
notched phones (web/PWA), the emoji-pack and theme-import dialogs fit the
screen, the call view's resize handle is disabled on phones, and icon buttons
meet a minimum touch height.
